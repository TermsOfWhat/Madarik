export const roundNumbersToFixed3 = (value: number | string): number => {
    return Number(Number(value).toFixed(3))
}

export const roundNumbersToFixed6 = (value: number | string): number => {
    return Number(Number(value).toFixed(6))
}

